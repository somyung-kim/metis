{
  "name": "metis",
  "version": "0.1.0",
  "description": "Claude Code plugin that delegates a coding task to Codex/Copilot CLI, remembers it, and uses that memory to inform the next delegation.",
  "type": "module",
  "license": "MIT",
  "author": "Somyung",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  }
}
